# Практическая работа №13: Обработка жестов

**Выполнил:**  
Саньков Андрей Александрович  
Группа: ИНС-б-о-24-1  
Направление: 09.03.02 «Информационные системы и технологии»

---

## Цель работы

Изучить механизмы обработки сенсорных жестов в Android. Научиться создавать собственные обработчики для распознавания свайпов (смахиваний) и других движений пальца по экрану. Интегрировать обработку жестов в игровое приложение (вариант – «Сапёр»).

---

## Ход работы

### Задание 1. Создание проекта и подготовка интерфейса
Обновлён экран игры (GameActivity). В разметку добавлен объект View (квадрат 100×100 dp) с оранжевым фоном, расположенный по центру экрана. Также оставлена кнопка «Завершить игру» для сохранения рекорда. Интерфейс готов для добавления жестов.

```xml
<View
    android:id="@+id/gameObject"
    android:layout_width="100dp"
    android:layout_height="100dp"
    android:background="#FF5722"
    android:layout_centerInParent="true" />
```
![](media/1.png)

**Рисунок 1** — Подготовка интерфейса
### Задание 2. Создание класса OnSwipeTouchListener
Разработан универсальный обработчик жестов, реализующий View.OnTouchListener. Внутри используется GestureDetector с переопределёнными методами:

onFling – распознаёт свайпы в четырёх направлениях (порог дистанции 100 px, скорость 100 px/s).

onLongPress – долгое нажатие.

onDoubleTap – двойное касание.

Класс имеет пустые публичные методы (onSwipeRight(), onSwipeLeft(), onSwipeTop(), onSwipeBottom(), onLongPress(), onDoubleTap()), которые переопределяются при использовании. Это позволяет легко назначать действия на жесты для любого View.`
```java
if (Math.abs(diffX) > Math.abs(diffY)) {
    if (Math.abs(diffX) > SWIPE_THRESHOLD && Math.abs(velocityX) > SWIPE_VELOCITY_THRESHOLD) {
        if (diffX > 0) onSwipeRight(); else onSwipeLeft();
    }
}
```
![](media/2.png)

**Рисунок 2** — Создание и наполнение класса OnSwipeTouchListener

### Задание 3. Применение обработчика жестов
В GameActivity на объект gameObject установлен экземпляр OnSwipeTouchListener с анонимной реализацией всех методов:

Свайпы перемещают квадрат на 100 px в соответствующем направлении.

Долгое нажатие меняет цвет квадрата на случайный (имитация установки флажка в «Сапёре»).

Двойной тап возвращает квадрат в центр экрана.

Добавлена визуальная обратная связь – при свайпе квадрат на 150 мс становится полупрозрачным.

Таким образом продемонстрирована интеграция жестов в игровой процесс, соответствующая индивидуальному варианту «Сапёр».

Пример кода обработчиков:
```java
gameObject.setOnTouchListener(new OnSwipeTouchListener(this) {
    @Override
    public void onSwipeRight() { moveObject(100, 0); provideFeedback(); }
    @Override
    public void onLongPress() { /* смена цвета */ }
    @Override
    public void onDoubleTap() { /* возврат в центр */ }
});
```
![](media/3.png)

**Рисунок 3** — Применение обработчика жестов

### Задание 4. Тестирование и настройка чувствительности
Приложение запущено и протестировано. Жесты распознаются уверенно, квадрат двигается без задержек. Экспериментально подобраны пороговые значения SWIPE_THRESHOLD = 100 и SWIPE_VELOCITY_THRESHOLD = 100, обеспечивающие комфортную работу. При необходимости их можно изменить для повышения или понижения чувствительности. Дополнительно проверена корректная работа всех жестов совместно с кнопкой «Завершить игру» – конфликтов нет.

## Контрольные вопросы
### 1. Что такое MotionEvent? Какие основные типы событий (actions) в нём существуют?
MotionEvent — это объект, который хранит всю информацию о касании экрана. Основные события:

ACTION_DOWN — палец коснулся экрана.
ACTION_MOVE — палец двигается по экрану.
ACTION_UP — палец оторвался от экрана.
ACTION_CANCEL — жест прерван (например, пришло уведомление).

### 2. Для чего используется GestureDetector? В чём его преимущество?
GestureDetector упрощает распознавание сложных жестов (свайпы, долгое нажатие, двойной тап). Преимущество: не нужно вручную высчитывать координаты и скорость — класс сам анализирует последовательность MotionEvent и вызывает готовые методы.

### 3. Какой метод GestureDetector отвечает за свайп? Какие параметры принимает?
За свайп отвечает метод onFling(MotionEvent e1, MotionEvent e2, float velocityX, float velocityY).
Параметры: точка начала жеста (e1), точка конца (e2), скорость по X и Y в пикселях/сек.

### 4. Зачем в методе onDown() возвращать true?
Если onDown() вернёт false, система не будет передавать этому слушателю последующие события (MOVE, UP, FLING). Возврат true говорит: «Я обрабатываю этот жест, дайте мне остальные события».

### 5. Как отличить горизонтальный свайп от вертикального?
Сравниваем абсолютные значения разницы координат: если |diffX| > |diffY| — горизонтальный, иначе вертикальный. Затем проверяем, превышает ли расстояние и скорость пороговые значения.

### 6. Что такое пороговые значения (threshold) и зачем они нужны?
Threshold — это минимальные значения расстояния и скорости, при которых движение считается свайпом. Они отсекают случайные мелкие дрожания пальца, чтобы свайп срабатывал только на осознанные движения.

### 7. Как заставить View реагировать на сенсорные события? Какой слушатель для этого используется?
Нужно установить слушатель View.OnTouchListener через view.setOnTouchListener(...). В методе onTouch(View v, MotionEvent event) обрабатываются все касания.

### 8. Какие ещё жесты можно распознать с помощью GestureDetector? Назовите не менее трёх.

onLongPress — долгое нажатие.

onDoubleTap — двойное касание.

onScroll — прокрутка (перемещение пальца с отображением дистанции).

onSingleTapConfirmed — одиночное касание (подтверждённое, что это не часть двойного).
